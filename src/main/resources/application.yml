spring:
  application:
    name: ExchangeXP

  data:
    mongodb:
      uri: ${MONGO_URI}
#      host: localhost
#      port: 27017
      database: ExchangeXP
      auto-index-creation: true

logging:
  level:
    com:
      Shinigami_Coderz:
        ExchangeXP:
          com.Shinigami_Coderz.ExchangeXP.ExchangeXPt: trace

firebase:
  service-account-file: ${FIREBASE_JSON}


springdoc:
  swagger-ui:
    path : /docs
  api-docs:
    path : /api-docs

server:
  servlet:
    context-path: /exchangexp/api/
